#Ask user for their name
name =  input("What's is your name? ").strip().title()

#Remove whitespace from str and capitalize the user's name
#name = name.strip().title() #Merge the strip and title

#Capitalize user's name
#name = name.title()  -- 1st approach

#Say hello to user
print (f"hello, {name}")        #Use data type to clean the data 